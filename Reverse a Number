#include <stdio.h>
void main()
{
  int n, r, rev = 0;

  printf("Enter a number = ");
  scanf("%d", &n);

  while (n != 0)
  {
    r = n % 10;
    rev = rev * 10 + r;
    n = n/10;
  }

  printf("Reverse of the number = %d\n", rev);

}


/*


n=728
r=8
rev=0*10+8
rev=8
n=728/10
n=72*/
